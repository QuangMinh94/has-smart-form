export type CriteriaDB = {
    userId: string
    userName?: string
    taskId: string
    name?: string
    category?: string
    currency?: string
    expected?: number
    actual?: number
    criteriaId?: string
}
