import type { ThemeConfig } from "antd"

const theme: ThemeConfig = {
    components: {
        Button: {
            colorPrimary: "#0E7490"
        }
    },
    token: {
        colorPrimary: "#0E7490"
    }
}

export default theme
